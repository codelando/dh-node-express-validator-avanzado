<!DOCTYPE html>
<html lang="en">
<%- include('../partials/head') %>
<body>
    <%- include('../partials/header') %>
    <main>
        <% if(user) { %>
            <%- include('user', { user }) %>
        <% } else { %>
            <h2>No encontramos ese grupo</h2>
        <% } %>
    </main>
</body>
</html>